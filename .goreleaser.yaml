# .goreleaser.yaml
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/kest
    binary: kest
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    # 定义 binary 的命名格式，方便 install.sh 识别
    id: kest
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64

archives:
  - format: tar.gz
    # 让文件名符合 install.sh 的逻辑，或者 install.sh 逻辑配合这里
    name_template: >-
      {{ .ProjectName }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

# Homebrew 自动更新配置
brews:
  - name: kest
    repository:
      owner: kest-lab
      name: homebrew-kest
    homepage: "https://github.com/kest-lab/kest-cli"
    description: "The CLI-first API Testing Tool for Vibe Coding"
    license: "MIT"
    test: |
      system "#{bin}/kest version"
    install: |
      bin.install "kest"
