---
title: API Spec Lifecycle
description: How specs move from local authoring to managed workspace resources.
---

# API spec lifecycle

Kest treats API specs as a working artifact rather than a static upload.

## Typical lifecycle

1. Author or discover endpoints locally.
2. Sync them into a workspace through Kest Cloud Sync or create them manually through the platform API.
3. Refine summaries, descriptions, examples, and generated docs.
4. Export the managed spec or derive test cases from it.

## Related API areas

- Workspaces
- Kest Cloud Sync
- API Specs
- Test Cases
